package org.wysko.midis2jam2.instrument

interface DeferredLocationBehavior {
    fun applyLocationBehavior(index: Float)
}

interface DeferredHarmonicInstanceLocationBehavior {
    fun applyLocationBehavior(index: Float)
}
